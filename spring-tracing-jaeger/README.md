## Spike to see how logs can be send to jaeger spans

App contains a single `/ping` endpoint. Start yaeger and check the 
Jaeger UI at `localhost:16686`

### Start the App

- `./gradle bootRun`

### Start Yeager Local

- `docker-compose up`
